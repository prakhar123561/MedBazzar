import makeStyles from '@mui/styles/makeStyles'

export const useStyles = makeStyles({
    root: {
        display: 'flex',
      justifyContent: 'center',
      alignItems: 'center',
      background: '#fff',
      fontFamily: 'Kanit',
      width: '100%',
      height: '100vh',
      background: '#32aeb1',
    },
    box: {
        background: '#fff',
        width: 600,
        height: 250,
        borderRadius: 10,
        padding: 10,
    }
})